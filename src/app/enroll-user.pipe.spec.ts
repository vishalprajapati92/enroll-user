import { EnrollUserPipe } from './enroll-user.pipe';

describe('EnrollUserPipe', () => {
  it('create an instance', () => {
    const pipe = new EnrollUserPipe();
    expect(pipe).toBeTruthy();
  });
});
